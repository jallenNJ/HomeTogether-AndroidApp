<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.ramapo.jallen6.hometogether">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HouseholdSelection" />
        <activity android:name=".NewHouseHoldForm"

            android:parentActivityName="edu.ramapo.jallen6.hometogether.HouseholdSelection">
        <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edu.ramapo.jallen6.hometogether.HouseholdSelection" /></activity>

        <activity android:name=".Household" />
        <activity android:name=".Pantry"  android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".PantryItemForm"></activity>
    </application>

</manifest>